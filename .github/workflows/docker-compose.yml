name: Deploy Flask API

on: 
  push:
    branches:
      - main

jobs:
  login:
    runs-on: ubuntu-latest
    steps:
      -
        name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: hanaparthi
          password: dckr_pat_2GmvHldBFEDUUhvpjTjLSlk7Tnw
      - 
        name: Build and push Docker image 
        run: docker build -t hanaparthi/chatapi:latest . 
      - 
        name: Run Docker container
        run: docker run -d -p 3000:3000 hanaparthi/chatapi:latest
